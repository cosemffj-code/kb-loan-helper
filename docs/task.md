제공해주신 PRD와 TRD를 바탕으로 프로젝트 개발을 위한 단위 작업(Task) 목록을 설계하였습니다. 

난이도는 최저 1점에서 최고 10점으로 평가하였으며, **기존에 8점 이상으로 분류되었던 고난이도 복합 작업(금융 엔진 통합 개발, 클라우드 동기화 오프라인 처리 등)은 하위 작업(Sub-task)으로 분리**하여 모든 단위 작업이 최대 7점을 넘지 않도록 구성하였습니다.

---

# 프로젝트 개발 태스크 목록 (Task List)

## 1단계: 개발 환경 구축 및 기본 설정

### Task 1: 프로젝트 초기화 및 도구 설정
* **요구 사양**: React 19 + TypeScript + Vite 개발 환경 세팅, 디렉토리 구조 생성
* **수행 내용**:
  * Vite를 이용한 React 19 + TypeScript 템플릿 생성
  * `KB_이름_여신도우미` 요구 폴더 구조(`src/lib`, `src/components`, `docs`, `.cursor` 등) 세팅
  * TailwindCSS 의존성 설치 및 `tailwind.config.js` 설정
* **난이도**: **3 / 10**

### Task 2: Vitest 단위 테스트 실행 환경 구축
* **요구 사양**: 프로젝트 내부 단위 테스트 실행 환경 마련
* **수행 내용**:
  * Vitest 및 `@testing-library/react` 의존성 설치
  * `vite.config.ts` 내 테스트 실행을 위한 커스텀 설정 추가
  * 간단한 더미 테스트 파일을 생성하여 테스트 인프라 동작 확인
* **난이도**: **4 / 10**

---

## 2단계: 핵심 금융 연산 엔진 개발 (TDD 방식 지향)
> 기존 "금융 로직 총괄 설계 및 검증(난이도 8)" 작업을 3개의 독립된 Sub-task로 세분화하였습니다.

### Task 3-1 (Sub-task): 대출 상환 연산 코어 로직 구현 (`src/lib/finance.ts`)
* **요구 사양**: 원리금균등상환 및 상환 스케줄 산출 계산 함수 작성
* **수행 내용**:
  * `calculateEqualPrincipalAndInterest` 순수 함수 개발
  * 사용자가 지정한 공식 적용: `M = P * r * (1 + r)^n / ((1 + r)^n - 1)`
  * 루프 연산 구조 작성: 이자(`잔액 * r`), 원금(`M - 이자`), 잔액(`잔액 - 원금`) 산출
  * 연이자율 0% 입력 시의 예외 대응 로직 추가
* **난이도**: **5 / 10**

### Task 3-2 (Sub-task): 예적금 만기 수령액 연산 로직 구현 (`src/lib/finance.ts`)
* **요구 사양**: 예적금 단리/복리 계산 함수 작성
* **수행 내용**:
  * 적금 단리 계산 공식 기반의 만기 수령액 산출 함수 작성
  * 적금 월복리 계산 공식 기반의 만기 수령액 산출 함수 작성
  * 세금 연산 생략 또는 일반 과세(15.4%) 적용 여부 조율용 매개변수 선언
* **난이도**: **5 / 10**

### Task 3-3 (Sub-task): 금융 로직 단위 테스트 코드 작성 및 검증
* **요구 사양**: 구현한 계산 공식들의 무결성 검증
* **수행 내용**:
  * `finance.test.ts` 파일을 생성하여 대출 계산 테스트 작성
  * 원금 1,000만 원, 연 6%, 12개월 입력 시 실제 상환 원리금 결과와 마지막 회차 잔액이 정확히 `0`원으로 끝나는지 부동 소수점 오차 검증
  * 단리 및 복리 예적금 계산 함수의 다양한 경계값 테스트 케이스 작성 및 통과 확인
* **난이도**: **4 / 10**

---

## 3단계: 기본 컴포넌트 및 UI 마크업 개발

### Task 4: 대시보드 구조 및 반응형 레이아웃 설계 (`src/App.tsx`)
* **요구 사양**: 한 화면에서 모든 도구를 전환할 수 있는 반응형 쉘 구성
* **수행 내용**:
  * 탭 상태(대출 계산기 / 예적금 계산기 / 상담 메모) 관리 기능 구현
  * 반응형 레이아웃(모바일 1열, 데스크톱 그리드 분할 구조) 설계
  * 상태 토글을 활용한 웹 사이트 전체 다크모드 적용 클래스 제어
* **난이도**: **5 / 10**

### Task 5: 대출 상환 계산기 폼 구현 (`src/components/LoanCalculator.tsx`)
* **요구 사양**: 사용자 입력을 받아 계산을 요청하는 인터페이스 구축
* **수행 내용**:
  * 대출금액, 연이자율, 대출기간 입력 인풋(Input) 제작
  * 천 단위 입력 콤마 기능 구현 및 "원" 단위 바인딩 표시
  * 입력 시 유효성 검사 구현 (예: 음수 제한, 기간 범위 제한 등)
* **난이도**: **4 / 10**

### Task 6: 월별 상환 스케줄 출력 컴포넌트 개발 (`src/components/RepaymentTable.tsx`)
* **요구 사양**: 계산 결과를 받아 상세 표 형식으로 테이블 구성
* **수행 내용**:
  * 회차, 납입 원금, 이자, 잔액을 표현하는 스크롤 영역이 포함된 테이블 마크업
  * 결과 금액에 천 단위 콤마(`,`) 및 "원" 표시 유틸리티 연동
  * 대출 계산이 실행되기 전 빈 화면 상태 가이드 문구(Placeholder) 구성
* **난이도**: **4 / 10**

### Task 7: 상담 메모 관리 컴포넌트 및 로컬 저장소 개발 (`src/components/ConsultationMemo.tsx`, `src/lib/storage.ts`)
* **요구 사양**: 실시간 로컬 메모 저장, 검색 및 삭제
* **수행 내용**:
  * 메모 입력 및 삭제 UI 구현
  * 검색창 입력 문자열 기반의 메모 목록 실시간 검색 필터링 구현
  * `localStorage`에 데이터 동기화 처리 유틸리티 함수 연동 및 새로고침 유지 확인
* **난이도**: **6 / 10**

---

## 4단계: 고급 기능 추가 (보강 기능)

### Task 8: 데이터 CSV 내보내기 및 인쇄 기능 개발 (`src/components/RepaymentTable.tsx`)
* **요구 사양**: 계산 결과의 외부 문서화 기능 제공
* **수행 내용**:
  * 스케줄 배열 데이터를 콤마로 구분된 CSV 문자열로 변환하여 다운로드하는 기능 제작
  * CSS의 `@media print` 쿼리를 적용하여 상담 화면 중 계산 결과 테이블 부분만 종이로 깔끔하게 출력되는 인쇄 레이아웃 구현
* **난이도**: **5 / 10**

---

## 5단계: Supabase 클라우드 데이터 동기화 개발
> 기존 "클라우드 양방향 실시간 동기화 및 동시성 대응(난이도 9)" 작업을 3개의 단계별 Sub-task로 분류하여 난이도를 낮췄습니다.

### Task 9-1 (Sub-task): Supabase 프로젝트 세팅 및 테이블 정의
* **요구 사양**: 클라우드 데이터베이스 초기 환경 세팅
* **수행 내용**:
  * Supabase 가입 및 데이터베이스 신규 프로젝트 설정
  * `memos` 테이블 생성 및 컬럼 정의 (`id`, `title`, `content`, `created_at` 등)
  * 인증 절차 생략을 위한 간단한 Row Level Security (RLS) 비활성화 혹은 공용 정책 수립
* **난이도**: **4 / 10**

### Task 9-2 (Sub-task): Supabase 연동 메모 API 작성
* **요구 사양**: API를 이용해 클라우드 DB에 데이터를 조회 및 변경하도록 연동
* **수행 내용**:
  * 프로젝트 내 Supabase JS Client 패키지 설치 및 API 키 연동
  * 메모 신규 등록(`insert`), 리스트 가져오기(`select`), 메모 삭제(`delete`) 함수 개발
* **난이도**: **5 / 10**

### Task 9-3 (Sub-task): 로컬스토리지 백업 및 온라인 동기화 전환 로직
* **요구 사양**: DB 차단 시 작동 정지 방지 및 오프라인-로컬 연결 제어
* **수행 내용**:
  * Supabase 서버 연결 불가 상태(오프라인) 발생 시, 에러 없이 `localStorage`로 자동 폴백(Fallback)하도록 설계
  * 네트워크 사용 가능 시 Supabase에 동기화 요청을 수행하는 구조적 핸들러 부착
* **난이도**: **7 / 10**

---

## 6단계: 빌드 및 실제 배포

### Task 10: 프로덕션 빌드 검증 및 Vercel 배포
* **요구 사양**: 실제 구동 환경 배포 및 배포 URL 확보
* **수행 내용**:
  * `npm run build` 명령어를 통한 배포용 프로덕션 번들 정상 동작 여부 최종 모니터링
  * Vercel 서비스 계정 연동 및 GitHub 저장소 연결
  * Supabase 연동에 필요한 환경 변수(`VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`) 설정 주입 후 정적 배포 수행
* **난이도**: **4 / 10**